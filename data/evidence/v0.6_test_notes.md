# v0.6 Test Notes — Detection Logging

## Test Purpose

Verify that the system logs real slot detection results at controlled time intervals without logging every frame.

## Expected Results

- [x] Program runs successfully
- [x] A1/A2/A3 detection still displays correctly
- [x] Detection results are written to `data/logs/detection_log.csv`
- [x] Log includes timestamp, slot name, status, edge pixels, and threshold
- [x] Logs are written periodically instead of every frame
- [x] Generated CSV log file is ignored by Git
- [x] Program exits cleanly when `q` is pressed

## Notes

v0.6 adds runtime detection logging while preserving the v0.5 config-based slot system.
